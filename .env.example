# Copy to .env and adjust if your backend runs elsewhere.
VITE_API_URL=http://localhost:5000/api

# Frontend-only mode: true = use built-in mock data (no backend needed),
# false = call the real Express backend at VITE_API_URL above.
VITE_USE_MOCK=true
